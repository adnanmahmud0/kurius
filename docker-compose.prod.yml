services:
  # ─────────────────────────────────────────
  # PostgreSQL — internal only, NOT exposed
  # ─────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: kurius_prod_postgres
    restart: always
    environment:
      POSTGRES_DB: kurius_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - kurius_prod_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d kurius_db"]
      interval: 10s
      timeout: 5s
      retries: 10
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - kurius_net

  # ─────────────────────────────────────────
  # Express API — accessible at localhost:5000
  # ─────────────────────────────────────────
  api:
    image: ghcr.io/${GH_REPOSITORY_OWNER:-adnanmahmud0}/kurius-api:latest
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: kurius_prod_api
    restart: always
    env_file: .env
    environment:
      # Override DATABASE_URL to use Docker internal network (service name = postgres)
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/kurius_db?schema=public
    ports:
      # Bind to localhost only — Nginx proxies from port 80/443
      - "127.0.0.1:5000:5000"
    volumes:
      - kurius_prod_uploads:/app/apps/api/uploads
    depends_on:
      postgres:
        condition: service_healthy
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - kurius_net

  # ─────────────────────────────────────────
  # Next.js Web — accessible at localhost:3000
  # ─────────────────────────────────────────
  web:
    image: ghcr.io/${GH_REPOSITORY_OWNER:-adnanmahmud0}/kurius-web:latest
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    container_name: kurius_prod_web
    restart: always
    env_file: .env
    environment:
      PORT: 3000
      HOSTNAME: "0.0.0.0"
    ports:
      # Bind to localhost only — Nginx proxies from port 80/443
      - "127.0.0.1:3000:3000"
    depends_on:
      - api
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - kurius_net

networks:
  kurius_net:
    driver: bridge

volumes:
  kurius_prod_pgdata:
    driver: local
  kurius_prod_uploads:
    driver: local
